# إعدادات قاعدة البيانات PostgreSQL
# قم بتعديل هذه المتغيرات لتتناسب مع إعداد قاعدة البيانات الخاصة بك
DATABASE_URL=postgres://username:password@localhost:5432/butterbakery

# متغيرات التطبيق
PORT=5000
NODE_ENV=development

# رمز سري للجلسات (عشوائي وطويل)
SESSION_SECRET=change_this_to_a_strong_random_secret

# إعدادات GitHub (للنشر عبر API)
GITHUB_TOKEN=your_github_token_here
GITHUB_REPO_INFO={"owner":"your_username","repo":"butterbakery-op","branch":"main"}

# إعدادات Render.com (للنشر المباشر)
RENDER_API_KEY=your_render_api_key_here

# المنفذ الافتراضي للواجهة الأمامية (Vite)
VITE_PORT=3000